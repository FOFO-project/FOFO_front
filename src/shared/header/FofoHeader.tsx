import { ReactElement, useReducer, useState } from "react";
import style from "./header.module.scss";

export function FofoHeader<T extends ReactElement> ({}) {
    return(
        <>
        
        </>
    );
}